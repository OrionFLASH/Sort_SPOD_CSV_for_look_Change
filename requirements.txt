# Зависимости Python для программы сортировки CSV файлов
# Установите их командой: pip install -r requirements.txt

# pandas - библиотека для работы с данными и анализа
# Используется для сложной обработки CSV файлов (если потребуется)
# Минимальная версия 1.5.0 для совместимости с Python 3.7+
pandas>=1.5.0

# python-dateutil - библиотека для работы с датами
# Используется для автоматического определения типа поля 'date'
# Автоматически парсит различные форматы дат без указания шаблона
# Минимальная версия 2.8.0 для стабильной работы
python-dateutil>=2.8.0
